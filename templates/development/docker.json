{
  "variables": {
    "ssh_host": "localhost",
    "ssh_port": "2222",
    "ssh_username": "vagrant",
    "ssh_private_key_file": "/Volumes/WDC/Projects/vagrants/aws_study/.vagrant/machines/default/virtualbox/private_key"
  },
  "builders": [
    {
      "type": "null",
      "ssh_host": "{{user `ssh_host`}}",
      "ssh_port": "{{user `ssh_port`}}",
      "ssh_username": "{{user `ssh_username`}}",
      "ssh_private_key_file": "{{user `ssh_private_key_file`}}"
    }
  ],

  "provisioners": [
    {
      "type": "ansible-local",
      "inventory_groups": "docker",
      "playbook_file": "provision/docker/docker.yml",
      "playbook_dir": "provision/docker"
    }
  ],
  "post-processors": []
}
